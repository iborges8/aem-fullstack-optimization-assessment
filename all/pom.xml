<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.assessment</groupId>
        <artifactId>aem-fullstack-optimization-assessment</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>assessment.all</artifactId>
    <packaging>content-package</packaging>
    <name>Assessment - All</name>

    <properties>
        <aem.host>localhost</aem.host>
        <aem.port>4502</aem.port>
        <aem.user>admin</aem.user>
        <aem.password>admin</aem.password>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.assessment</groupId>
            <artifactId>assessment.ui.apps</artifactId>
            <version>${project.version}</version>
            <type>zip</type>
        </dependency>
        <dependency>
            <groupId>com.assessment</groupId>
            <artifactId>assessment.ui.config</artifactId>
            <version>${project.version}</version>
            <type>zip</type>
        </dependency>
        <dependency>
            <groupId>com.assessment</groupId>
            <artifactId>assessment.ui.content</artifactId>
            <version>${project.version}</version>
            <type>zip</type>
        </dependency>
        <dependency>
            <groupId>com.assessment</groupId>
            <artifactId>assessment.core</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.jackrabbit</groupId>
                <artifactId>filevault-package-maven-plugin</artifactId>
                <version>${filevault.package.plugin.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <group>assessment</group>
                    <packageType>container</packageType>
                    <name>assessment.all</name>
                    <embeddeds>
                        <embedded>
                            <groupId>com.assessment</groupId>
                            <artifactId>assessment.ui.apps</artifactId>
                            <type>zip</type>
                            <target>/apps/assessment-packages/application/install</target>
                        </embedded>
                        <embedded>
                            <groupId>com.assessment</groupId>
                            <artifactId>assessment.ui.config</artifactId>
                            <type>zip</type>
                            <target>/apps/assessment-packages/application/install</target>
                        </embedded>
                        <embedded>
                            <groupId>com.assessment</groupId>
                            <artifactId>assessment.ui.content</artifactId>
                            <type>zip</type>
                            <target>/apps/assessment-packages/content/install</target>
                        </embedded>
                        <embedded>
                            <groupId>com.assessment</groupId>
                            <artifactId>assessment.core</artifactId>
                            <target>/apps/assessment-packages/application/install</target>
                        </embedded>
                    </embeddeds>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>autoInstallPackage</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.day.jcr.vault</groupId>
                        <artifactId>content-package-maven-plugin</artifactId>
                        <version>1.0.6</version>
                        <executions>
                            <execution>
                                <id>install-package</id>
                                <phase>install</phase>
                                <goals>
                                    <goal>install</goal>
                                </goals>
                                <configuration>
                                    <targetURL>
                                        http://${aem.host}:${aem.port}/crx/packmgr/service.jsp</targetURL>
                                    <userId>${aem.user}</userId>
                                    <password>${aem.password}</password>
                                    <failOnError>true</failOnError>
                                    <timeout>30</timeout>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
